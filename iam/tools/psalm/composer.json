{
    "require-dev": {
        "psalm/plugin-symfony": "^5.1",
        "vimeo/psalm": "^5.20"
    },
    "config": {
        "preferred-install": {
            "*": "dist"
        },
        "sort-packages": true
    }
}
