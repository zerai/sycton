{
    "require": {
        "php": ">=8.1"
    },
    "require-dev": {
        "psalm/plugin-symfony": "^5",
        "vimeo/psalm": "^5.4"
    },
    "config": {
        "preferred-install": {
            "*": "dist"
        },
        "sort-packages": true
    }
}
